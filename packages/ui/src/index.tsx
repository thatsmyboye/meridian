/**
 * @meridian/ui — Shared component library
 *
 * Cross-platform UI primitives for web (Next.js) and mobile (Expo).
 * Components use React-only APIs here; platform-specific variants
 * (e.g. React Native primitives) are introduced in E08.
 */

export { Button } from "./components/Button";
export type { ButtonProps } from "./components/Button";

export { Text } from "./components/Text";
export type { TextProps } from "./components/Text";
